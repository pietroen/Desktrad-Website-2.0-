/*
 * EMAILJS CONFIGURATION
 *
 * Put your EmailJS browser/public values in the three fields below.
 * These values are safe to use in frontend code; NEVER put an EmailJS private key here.
 *
 * 1) publicKey: EmailJS Dashboard -> Account -> Public Key
 * 2) serviceId: EmailJS Dashboard -> Email Services
 * 3) templateId: EmailJS Dashboard -> Email Templates
 */
window.DESKTRAD_EMAILJS = {
    publicKey: 'YOUR_PUBLIC_KEY',
    serviceId: 'YOUR_SERVICE_ID',
    templateId: 'YOUR_TEMPLATE_ID'
};
